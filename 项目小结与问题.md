# 学习记忆助手 —— 项目小结与问题记录

## 一、项目小结

### 1.1 完成情况

经过约一周的开发，完成了以下模块：

| 模块 | 状态 | 代码量 |
|------|------|--------|
| 卡片管理（CRUD + 搜索 + 收藏） | ✅ | 311行 |
| 间隔重复复习 | ✅ | 207行 |
| 遗忘曲线可视化 | ✅ | 349行 |
| 学习统计 | ✅ | 172行 |
| 番茄钟 | ✅ | 263行 |
| 记忆森林 | ✅ | 308行 |
| 费曼学习法 AI 对话 | ✅ | 198行 + 96行 Service |
| 知识图谱 | ✅ | 460行 |
| 识文件/识图片导入 | ✅ | 154行 Service + 289行 UI |
| 首页仪表盘 | ✅ | 165行 |
| 玻璃拟态主题 | ✅ | CSS 约320行 |

**总计**：约 3500 行 Java 代码 + 300 行 CSS

### 1.2 技术收获

1. **JavaFX 桌面开发**：掌握了 BorderPane/StackPane/VBox/HBox 布局，ListView/TableView/ComboBox 等组件，Canvas 绘图，CSS 样式系统
2. **数据持久化**：使用 Gson 实现 JSON 序列化/反序列化
3. **API 集成**：调用 DeepSeek API 实现 AI 对话和文件内容分析
4. **Canvas 绘图**：实现了遗忘曲线、知识图谱力导向布局、记忆森林手绘树等可视化功能
5. **交互设计**：实现了缩放、拖拽、悬停高亮、点击切换等交互

---

## 二、遇到的问题与解决方案

### 问题 1：CSS 渐变语法差异

**现象**：CSS 中 `linear-gradient(135deg, ...)` 在 JavaFX 中报错  
**原因**：JavaFX CSS 不支持 Web 的渐变语法  
**解决**：改为 `linear-gradient(from 0% 0% to 100% 100%, ...)`  
**经验**：JavaFX CSS 不是 Web CSS，语法需要查官方文档

### 问题 2：Color.rgb alpha 值溢出

**现象**：运行时抛出 `IllegalArgumentException: Color's opacity value must be in 0.0-1.0`  
**原因**：`Color.rgb(r, g, b, alpha)` 的第四个参数范围是 0.0~1.0，而非 0~255  
**解决**：将所有 alpha 值改为 0.0~1.0 小数  
**经验**：注意 API 参数范围，多看官方文档

### 问题 3：Canvas 未初始化时绘制

**现象**：程序启动时报除零异常  
**原因**：`AnimationTimer` 在 Canvas 尚未完成布局时就开始绘制，此时宽高为 0  
**解决**：在 `draw()` 开头加 `if (W < 100 || H < 100) return;`  
**经验**：动画循环中必须考虑组件的初始化状态

### 问题 4：知识图谱节点重叠

**现象**：卡片增多时所有节点挤在一起  
**原因**：随机布局无排斥力  
**解决**：实现力导向布局（互斥力 + 吸引力），缩放时同步展开  
**经验**：可视化中物理模拟比纯随机效果好得多

### 问题 5：OcrDialog 卡片列表文字不可见

**现象**：CheckBox 列表项文字看不清  
**原因**：ListView 默认 cell 样式与深色背景不兼容  
**解决**：自定义 `ListCell`，设置白色文字  
**经验**：深色主题需要手动覆盖所有组件的默认样式

### 问题 6：API 响应流重复消费

**现象**：AI 接口返回空内容  
**原因**：`Scanner` 调用了两次 `useDelimiter` 导致流被消费完  
**解决**：改为一次调用并用变量保存结果  
**经验**：流只能消费一次

---

## 三、待改进之处

1. **知识图谱加载速度**：卡片过多时力导向布局计算量较大，可做异步计算
2. **离线支持**：目前 AI 功能依赖网络，可考虑本地词库离线翻译
3. **数据导出**：支持导出为 TXT/CSV 格式
4. **多用户**：支持多个学习计划切换
5. **今日复习目标设置**：用户自定义每天复习目标

---

## 四、运行说明

1. **环境要求**：JDK 17+，Maven
2. **运行命令**：`mvn javafx:run`
3. **数据存储**：`~/.memory-assistant/flashcards.json`
4. **API 配置**：首次使用"识文件"功能时输入 DeepSeek API Key
