# 学习记忆助手 —— 核心代码讲解

---

## 1. FlashCard.java —— 数据模型

**位置**：`model/FlashCard.java`

这是整个应用的数据基础。每一张卡片就是一个 `FlashCard` 对象。

### 核心字段：

```java
private int stage;          // 记忆阶段 0-5，核心字段
private String nextReviewDate;  // 下次复习日期
private int easyCount;      // 记得次数
private int mediumCount;    // 模糊次数
private int hardCount;      // 不记得次数
private boolean starred;    // 是否收藏
```

### 关键方法：

```java
public boolean isDue(LocalDate today) {
    // 判断今天是否需要复习：下次复习日期 <= 今天
    if (nextReviewDate == null || nextReviewDate.isEmpty()) return false;
    return !LocalDate.parse(nextReviewDate).isAfter(today);
}
```

### 讲解要点：
- `stage` 是遗忘曲线算法的输入，范围 0~5
- Gson 通过 getter/setter 自动序列化/反序列化，所以每个字段都需要 get/set
- `isDue()` 是整个复习流程的触发条件

---

## 2. SpacedRepetition.java —— 核心算法

**位置**：`service/SpacedRepetition.java`

这是**艾宾浩斯遗忘曲线**的具体实现，是项目的理论核心。

### 间隔数组：

```java
public static final int[] INTERVALS = {0, 1, 3, 7, 15, 30};
```

对应 stage 0→5，间隔递增。

### 核心方法：

```java
public static ReviewResult calculate(int stage, Difficulty diff, LocalDate today) {
    int newStage;
    switch (diff) {
        case EASY:   newStage = Math.min(stage + 1, 5);  break;
        case MEDIUM: newStage = stage;                    break;
        case HARD:   newStage = Math.max(stage - 1, 0);   break;
    }
    LocalDate nextDate = today.plusDays(INTERVALS[newStage]);
    return new ReviewResult(newStage, nextDate);
}
```

### 讲解要点：
- **EASY**：说明掌握了，进入更长间隔（stage+1）
- **MEDIUM**：不太熟练，保持当前间隔
- **HARD**：忘记了，缩短间隔（stage-1）
- **ReviewResult** 返回新的 stage 和下次复习日期
- 这是纯函数，无副作用，易于测试

---

## 3. DataStore.java —— 数据持久化

**位置**：`storage/DataStore.java`

Gson 实现 JSON 文件存储。

```java
public static List<FlashCard> loadCards() {
    // 读取 ~/.memory-assistant/flashcards.json
    // 文件不存在返回空列表
    // Gson 自动将 JSON 数组转为 List<FlashCard>
}

public static void saveCards(List<FlashCard> cards) {
    // 将卡片列表转为 JSON 写入文件
    // setPrettyPrinting() 使 JSON 可读
}
```

### 讲解要点：
- 使用静态方法，简化调用
- 文件存到用户目录 `~/.memory-assistant/`，与项目分离
- 每次切换页面都重新加载，保证数据一致性

---

## 4. ReviewView.java —— 复习流程

**位置**：`ui/ReviewView.java`

复习的核心流程：取 due 卡片 → 展示正面 → 用户回忆 → 展示背面 → 评级 → 保存结果。

```java
public void startReview() {
    LocalDate today = LocalDate.now();
    List<FlashCard> allCards = DataStore.loadCards();
    // 筛出 today 之前到期的卡片
    cards = allCards.stream()
            .filter(c -> c.isDue(today))
            .collect(Collectors.toList());
    currentIndex = 0;
    showCard();
}
```

评级时记录次数并更新 stage：

```java
private void rateCard(SpacedRepetition.Difficulty diff) {
    // 记录评级次数
    switch (diff) {
        case EASY:   card.setEasyCount(card.getEasyCount() + 1);   break;
        case MEDIUM: card.setMediumCount(card.getMediumCount() + 1); break;
        case HARD:   card.setHardCount(card.getHardCount() + 1);   break;
    }
    // 计算新的 stage 和下次复习日期
    ReviewResult result = SpacedRepetition.calculate(card.getStage(), diff, today);
    card.setStage(result.newStage);
    card.setNextReviewDate(result.nextReviewDate.toString());
    // 立即写盘
    DataStore.saveCards(allCards);
}
```

### 讲解要点：
- 每次评级立即写盘，防止崩溃丢失进度
- 三种评级都记录 `lastReviewDate`，都算"已复习"

---

## 5. GraphView.java —— 知识图谱（最复杂）

**位置**：`ui/GraphView.java`

### 力导向布局：

```java
// 互斥力使节点散开
double repulsiveForce = REPULSION / (dist * dist);
// 同分类吸引力维持分组
forces[i][0] += dx * ATTRACTION;
// 缩放时 spreadFactor 增大 → 互斥力增大
double repulsiveForce = REPULSION * spreadFactor / (dist * dist);
```

### 缩放与展开：

```java
canvas.setOnScroll(e -> {
    scale = newScale;
    spreadFactor = scale * 2;  // 缩放越大展开越开
    applyForceLayout();  // 重新计算节点位置
    draw();
});
```

### 讲解要点：
- 力导向布局使节点自然分散，避免重叠
- 缩放时同步调整物理布局，而非简单放大
- 双击弹出卡片详情

---

## 6. 关键技术难点

| 问题 | 解决方案 |
|------|---------|
| JavaFX CSS 不支持 Web 渐变语法 | 改用 `linear-gradient(from 0% 0% to 100% 100%)` |
| Color.rgb alpha 值溢出 | 第四参数改为 0.0~1.0 而非 0~255 |
| Canvas 渲染时宽高为 0 | 加 `if (W < 100) return;` 保护 |
| 知识图谱节点重叠 | 力导向布局 + 缩放时展开 |
| 内存森林点击 Tooltip 残留 | 改用底部固定 Label 显示信息 |
